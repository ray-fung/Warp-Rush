using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CanvasScript : MonoBehaviour {

    GameObject rocket;

	// Use this for initialization
    void Start ()  {
        //GameObject rocket = GameObject.Find("Rocket");
	}
	
	// Update is called once per frame
	void Update () {
        //if (rocket.GetComponent<RocketScript>().lives == 2)
        //{
        //    Destroy(this.transform.FindChild("Life 1"));
        //}

        //if (rocket.GetComponent<RocketScript>().lives == 1)
        //{
        //    Destroy(this.transform.FindChild("Life 2"));
        //}

        //if (rocket.GetComponent<RocketScript>().lives == 0)
        //{
        //    Destroy(this.transform.FindChild("Life 3"));
        //}

    }
}
